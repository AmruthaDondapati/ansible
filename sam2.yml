- name: pay1
  hosts: dev
  tasks:
    - name: patching needs to be done on below servers.
      ansible.builtin.debug:
        msg: r1 
- name: pay2
  hosts: qa
  tasks:
    - name: patching needs to be done on below servers.
      ansible.builtin.debug:
        msg: r2
